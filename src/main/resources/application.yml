bot:
  token: ${BOT_TOKEN}
  welcome-message: "Привет! Подключайся к нашему <a href=\"https://t.me/MintDatingBot/Mint\">приложению!</a>"

spring:
  config.import: optional:file:.env[.properties]

logging:
  level:
    root: DEBUG
    okhttp3.*: ERROR
    org.apache.*: ERROR
    javax.*: ERROR
    sun.rmi.*: ERROR