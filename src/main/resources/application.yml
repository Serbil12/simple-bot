bot:
  token: ${BOT_TOKEN}
  message: "Привет! Подключайся к нашему приложению!"

spring:
  config.import: optional:file:.env[.properties]

logging:
  level:
    root: DEBUG
    okhttp3.*: ERROR
    org.apache.*: ERROR
    javax.*: ERROR
    sun.rmi.*: ERROR